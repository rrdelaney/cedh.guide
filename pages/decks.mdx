import { makePageLayout } from '../components/page_layout';
import { translationProps } from '../lib/translations';

# What are cEDH decks like?

[EDHTop16](https://edhtop16.com) compiles results for various commander at top-level tournaments and is
very representitive of the overall metagame. It's a great resource for learning what to expect from
cEDH games across the world. Explore the lists and find a deck you want to try out!

export default makePageLayout({
  title: 'What are cEDH decks like?',
});

export const getStaticProps = translationProps(['common']);
